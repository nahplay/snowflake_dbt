version: 2
models:
- name: bikes_downstream_to_staging
  description: This is a downstream model to bikes_staging.
  columns:
    - name: downstream_model
      data_type: string 
      description: Downstream indicator
      tests:
        - not_null
    - name: registered_count_ratio
      data_type: not_null
      description: Some ration to test
      tests:
        - not_null